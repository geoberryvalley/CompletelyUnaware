# Completely Unaware
* Small program I made to see if I could automate a swing trading strategy
* Used psycopg3 to record transaction history
* Used OpenAI's API to do stock filtering
* Used fetch() to grab data from market data APIs
