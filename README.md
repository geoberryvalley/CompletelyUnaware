# Completely Unaware
* Small program I made to see if I could automate a swing trading strategy, changed into a discord bot for paper trading
* Used psycopg3 to record transaction history (with connection pool)
* Used OpenAI's API to do stock filtering
* Used fetch() to grab data from market data APIs
* Used pycord to integrate it with discord
